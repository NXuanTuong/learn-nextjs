import React from 'react'
import LayoutEmpty from '../components/Layout/empty'

type Props = {}

const NotFound = (props: Props) => {
  return (
    <div>NotFound 400</div>
  )
}

NotFound.Layout = LayoutEmpty

export default NotFound